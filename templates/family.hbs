{{#each family}}
<div class="row fams" data-key="{{@key}}">
  <div class="col-xs-6 col-md-6 page-header" >
    <h3>{{name}} <small>{{age}}</small></h3>
    <h6>{{gender}}</h6>
    <h5>{{skills}}</h5>
    <button  class="btn btn-danger btn-xs delButton">Delete</button>
  </div>
  <div class="col-xs-6 col-md-6"></div>
</div>
{{/each}}